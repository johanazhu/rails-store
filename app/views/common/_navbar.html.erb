

<nav class="bg-light border-bottom">
    <div class="container d-flex flex-wrap">
      <ul class="nav me-auto">
        <li class="nav-item"><a href="/" class="nav-link link-dark px-2 active" aria-current="page">JDStore</a></li>
      </ul>
      <ul class="nav">
        <% if !current_user %>
        <li class="nav-item">
            <%= link_to("注册", new_user_registration_path, class: "nav-link link-dark px-2") %>
        </li>
        <li class="nav-item">
            <%= link_to("登入", new_user_session_path, class: "nav-link link-dark px-2") %>
        </li>
        <% else %>
            <li class="dropdown">
                <a href="#" class="nav-link dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
                    Hi!, <%= current_user.email %>
                    <b class="caret"></b>
                </a>
                <ul class="dropdown-menu">
                    <li> <%= link_to("登出", destroy_user_session_path, method: :delete, class: "dropdown-item") %> </li>
                </ul>
            </li>
        
        <% end %>
      </ul>
    </div>
</nav>